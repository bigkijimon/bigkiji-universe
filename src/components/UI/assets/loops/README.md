# loops/ — 背景ビデオループ受け口

ここに `.mp4` / `.webm` を置くと、シナプスキャンバスの背景レイヤーとして自動再生される
（screen合成・opacity 0.22・最下層。先頭の1本のみ使用）。

将来 ComfyUI / Wan2.2 / LTX-2 で背景動画ループを生成したらここに置くだけでよい。
現在は空＝ComfyUI生成静止画（bg-synapse-field.jpg）+シェーダアニメで動きを表現している。
